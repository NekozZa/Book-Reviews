<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/remixicon@4.3.0/fonts/remixicon.css" rel="stylesheet"/>
    <link rel="stylesheet" href="/styles/new.css">
    <title>New Book</title>
</head>
<body>
    <form action="/add" method="post">
        <div class="header">
            <h1>Book Infos</h1>
            <hr>

            <input type='text' name="author" placeholder="Author" value= '<%= locals.book ? book.author : ''%>'>
            <input type="text" name="book_code" placeholder="ISBN" value= '<%= locals.book ? book.book_code : ''%>'>
            <input type="text" name="title" placeholder="Title" value= '<%= locals.book ? book.title : ''%>'>
        </div>

        <textarea name="review" class="body" placeholder="My review..."><%= locals.book ? book.review : '' %></textarea>
    
        <div class="footer">
            <div>
                <label for="score">Recommendation: </label>
                <input type="number" min="1" max="5" step="0.1" name="score" value= '<%= locals.book ? book.score : ''%>'>
            </div>
            
            <button type="submit"><i class="ri-expand-right-fill"></i></button>
        </div>
    </form>
</body>
</html>